# AutoDock Vina 1.2.5 Configuration
# Target: MED13L CDK8-interaction domain (residues 100-510)
# Structure: AlphaFold AF-Q71F56-F1, model v6

receptor = MED13L_CDK8_domain.pdbqt

# Grid box centred on CDK8-interaction domain
# NOTE: Coordinates should be calculated from the domain centroid
#       of residues 100-510 in the AlphaFold structure.
#       Use PyMOL: centerofmass resi 100-510
# center_x = [SET AFTER RECEPTOR PREPARATION]
# center_y = [SET AFTER RECEPTOR PREPARATION]
# center_z = [SET AFTER RECEPTOR PREPARATION]

# Grid dimensions (Angstroms)
size_x = 40
size_y = 40
size_z = 40

# Spacing (Angstroms)
# spacing = 0.375  # Note: Vina uses fixed spacing internally

# Search parameters
exhaustiveness = 32
num_modes = 10
energy_range = 4

# Random seed for reproducibility
seed = 42
